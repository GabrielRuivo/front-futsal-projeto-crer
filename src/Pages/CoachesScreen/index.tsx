import * as React from 'react';
import { Text, View } from 'react-native';

export function CoachesScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Listagem dos treinadores!</Text>
    </View>
  );
}